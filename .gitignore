# KG Explorer — private data (never commit these)
private/
*.private.json

# Python
__pycache__/
*.pyc
.DS_Store

# Build artifact, regenerated by the deploy workflow against upstream JSON-LD.
# Local dev: rebuild with `python build_data.py --src ../csv-to-json-ld/remote/models/schema-jsonld`.
data.json
schema-jsonld/